import * as iconifyData from "@iconify-json/fa6-regular";
import { IconSet } from "../types";

const iconSet = new IconSet("fa6-regular", iconifyData.info, iconifyData.icons);
export default iconSet;
