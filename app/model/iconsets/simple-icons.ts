import * as iconifyData from "@iconify-json/simple-icons";
import { IconSet } from "../types";

const iconSet = new IconSet("simple-icons", iconifyData.info, iconifyData.icons);
export default iconSet;
